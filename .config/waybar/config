{
    // "layer": "top", // Waybar at top layer
    "position": "top", // Waybar position (top|bottom|left|right)
    // "height": 30, // Waybar height
    // "width": 1280, // Waybar width

    // Choose the order of the modules
    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": [],
    "modules-right": ["pulseaudio", "network", "temperature", "cpu", "memory", "disk", "backlight", "clock", "tray"],
    "sway/workspaces": {
        "disable-scroll": true,
        "all-outputs": false,
        "format": "{name}",
        "persistent_workspaces": {
            "1": [],
            "2": []
        }
    },
    "sway/mode": {
        "format": "<span><b>{}</b></span>"
    },
    "backlight": {
        // "device": "acpi_video1",
        "format": "{percent}% {icon}",
        "format-icons": ["", ""]
    },
    "battery": {
        "states": {
            // "good": 95,
            "warning": 30,
            "critical": 15
        },
        "format": "{capacity}% {icon}",
        "format-charging": "{capacity}% ",
        "format-plugged": "{capacity}% ",
        "format-alt": "{time} {icon}",
        // "format-good": "", // An empty format will hide the module
        // "format-full": "",
        "format-icons": ["", "", "", "", ""]
    },
    "pulseaudio": {
        "scroll-step": 5, // %, can be a float
        "format": "{icon} {volume}% {format_source}",
        "format-bluetooth": " {icon} {volume}% {format_source}",
        "format-bluetooth-muted": "  {format_source}",
        "format-muted": " {format_source}",
        "format-source": " {volume}%",
        "format-source-muted": "",
        "format-icons": {
            "default": ["", "", ""]
        },
        // scrolling changes volume
        "on-click": "pactl set-sink-mute $(pactl list short sinks | grep -i running | cut -f1) toggle",
        "on-click-right": "pactl set-source-mute $(pactl list sources short | grep -i input | cut -f1) toggle",
        "on-click-middle": "pavucontrol"
    },
    "network": {
        "format-wifi": " {ifname}: {essid} ({signalStrength}%)  {bandwidthUpOctets}  {bandwidthDownOctets}",
        "format-ethernet": " {ifname}  {bandwidthUpOctets}  {bandwidthDownOctets}",
        "format-linked": " {ifname}: Link  {bandwidthUpOctets}  {bandwidthDownOctets}",
        "format-disconnected": " Disconnected",
        "format": "{ifname}",
        "tooltip-format-wifi": "{ipaddr}/{cidr}",
        "tooltip-format-ethernet": "{ipaddr}/{cidr}",
        "tooltip-format-disconnected": "", // shows format-disconnected?
        "tooltip-format": "",
        "interval": 3
    },
    "temperature": {
        // "thermal-zone": 2,
        // "hwmon-path": "/sys/class/hwmon/hwmon2/temp1_input",
        "critical-threshold": 80,
        // "format-critical": "{temperatureC}°C {icon}",
        "format": "{icon} {temperatureC}°C",
        "format-icons": ["", ""],
        "interval": 10,
        "tooltip": false
    },
    "cpu": {
        "format": " {usage:2}%",
        "interval": 5,
        "states": {
            "warning": 50,
            "critical": 80
        },
        "tooltip": true
    },
    "memory": {
        "format": " {percentage}%",
        // "tooltip-format": "{used} of {avail}", // not available :(
        "interval": 5,
        "states": {
            "warning": 50,
            "critical": 80
        },
        "tooltip": true
    },
    "disk": {
        "format": " {percentage_used}%",
        "tooltip-format": "{path}: {used} of {total}",
        "interval": 30
    },
    "clock": {
        "timezone": "Europe/Berlin",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
        "format": "{:%T %m-%d-%Y}",
        "interval": 1
    },
    "tray": {
        "icon-size": 20,
        "spacing": 5
    }
}
